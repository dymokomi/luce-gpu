#prisma 4.0
def package "luce-gpu" {
    str owner = "dymokomi"
    str version = "0.1.4"
    str kind = "package"
    str language = "luce-base"
    str description = "GPU devices, textures, shaders and frames for Luce: Metal on macOS and Vulkan on Windows and Linux, presenting to luce-window windows."
    str readme = "README.md"

    def export "gpu" {
        str module = "luce_gpu.gpu"
    }

    def dependency "luce-std" {
        str owner = "dymokomi"
        str version = "^0.1.0"
        str path = "../luce-std"
    }

    def dependency "luce-window" {
        str owner = "dymokomi"
        str version = "^0.1.0"
        str path = "../luce-window"
    }

    def native "macos" {
        str[] libraries = ["objc"]
        str[] frameworks = ["Metal", "CoreGraphics", "QuartzCore"]
    }

    def native "windows" {
        str[] libraries = ["vulkan-1"]
        str[] link_search = ["$VULKAN_SDK/Lib"]
    }
}
